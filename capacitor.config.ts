import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unnamed',
  appName: 'unnamed',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
